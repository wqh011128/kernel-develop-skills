interface:
  display_name: "Kernel Tuning Loop"
  short_description: "Test single or repeated kernel optimizations"
  default_prompt: "Use $kernel-tuning-loop to test this kernel optimization with correctness, attribution, full-latency, overlap, and budget gates."

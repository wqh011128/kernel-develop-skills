interface:
  display_name: "Kernel Dev Lifecycle"
  short_description: "Route and close kernel work against repository CI"
  default_prompt: "Use $kernel-dev-lifecycle to select the smallest safe workflow and close this kernel task against AGENTS.md, GitHub CI, IR upload, and commit-message gates."

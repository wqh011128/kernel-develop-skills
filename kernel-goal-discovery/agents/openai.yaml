interface:
  display_name: "Kernel Goal Discovery"
  short_description: "Resolve material kernel contract unknowns"
  default_prompt: "Use $kernel-goal-discovery to inspect the real repository and resolve only the kernel decisions that block implementation."

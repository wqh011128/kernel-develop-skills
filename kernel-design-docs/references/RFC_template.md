# RFC 0000: <RFC 标题>

文件名格式：`<number>-<component>-<short-name>.md`

| 字段 | 内容 |
| --- | --- |
| Status | Draft / In Review / Accepted / Rejected / Superseded |
| Owner | <负责人> |
| Reviewers | <评审人> |
| Created | YYYY-MM-DD |
| Updated | YYYY-MM-DD |
| Target | <目标版本/里程碑/交付窗口> |

## 1. Summary

用 2-4 段说明本 RFC 要解决什么问题、提议新增或修改什么能力，以及为什么现在需要做。

建议包含：

1. 当前系统已经具备什么能力。
2. 当前缺少什么能力或存在什么约束。
3. 本 RFC 提议的核心变化。
4. 实现应尽量贴合的现有代码边界或组织边界。

示例结构：

```text
<系统/模块> 当前已经可以 <已有能力>，但缺少 <缺口>。

本 RFC 提议新增/调整以下能力：
1. <能力一>
2. <能力二>
3. <兼容性或预留能力>

实现应尽量贴合当前代码边界：
1. <模块 A> 负责 <职责>
2. <模块 B> 负责 <职责>
3. <模块 C> 复用 <既有能力>
```

## 2. Problem Statement

描述问题本身，而不是先描述方案。优先写清楚用户、系统或业务当前遇到的具体痛点。

| 工作域 | 现状痛点 | 业务影响 |
| --- | --- | --- |
| <领域一> | <当前问题> | <影响> |
| <领域二> | <当前问题> | <影响> |
| <领域三> | <当前问题> | <影响> |

具体问题包括：

1. <问题一>
2. <问题二>
3. <问题三>

## 3. Context

### 3.1 Current Behavior / Architecture

说明当前主流程、关键模块职责、数据流或控制流。

```text
<entrypoint>
  -> <step 1>
  -> <step 2>
  -> <step 3>
```

当前模块职责如下：

| 模块 | 当前职责 |
| --- | --- |
| `<module_a>` | <职责> |
| `<module_b>` | <职责> |
| `<module_c>` | <职责> |

当前行为中需要继续复用的能力：

1. <复用点一>
2. <复用点二>
3. <复用点三>

### 3.2 Relevant Background and Constraints

列出理解本 RFC 必须知道的背景、依赖、外部系统限制或顺序约束。

1. <约束一>
2. <约束二>
3. <约束三>

### 3.3 Technical Environment

列出和实现、测试、部署强相关的技术环境。

1. <语言/运行时版本>
2. <关键依赖版本>
3. <包管理/构建工具>
4. <测试框架>
5. <代码风格/CI 约束>

### 3.4 Positioning

用一小段话给概念定边界，避免后续 Review 中出现定义争议。

```text
本 RFC 中的 <关键概念> 指 <定义>，不表示 <非目标或容易混淆的定义>。
```

## 4. Goals

每个 Goal 只描述一个可验收的结果。Goal 应该能被测试、命令、产物或 Review 标准验证。

### Goal 1 — <目标名>

定位：<一句话说明这个目标解决什么问题。>

逻辑：

```text
<输入/配置>
  -> <处理步骤>
  -> <输出/产物>
```

启用方式：

```bash
<命令或配置示例>
```

验收标准：

1. <可验证标准一>
2. <可验证标准二>
3. <可验证标准三>

### Goal 2 — <目标名>

定位：<一句话说明这个目标解决什么问题。>

规则：

1. <规则一>
2. <规则二>
3. <规则三>

验收标准：

1. <可验证标准一>
2. <可验证标准二>

### Goal 3 — <兼容性/迁移/稳定性目标>

定位：<说明如何保持现有行为稳定。>

约束：

1. <兼容约束一>
2. <兼容约束二>
3. <兼容约束三>

## 5. Non-Goals

明确本 RFC 不做什么。Non-Goals 要具体，避免范围在实现阶段扩大。

1. 不 <非目标一>。
2. 不 <非目标二>。
3. 不 <非目标三>。
4. 不 <非目标四>。

## 6. Proposed Design

### 6.1 Responsibility Matrix

| # | 职责 | 优先级 | 频率 | 交付物 |
| --- | --- | --- | --- | --- |
| 1 | <职责一> | P0/P1/P2 | <触发频率> | <交付物> |
| 2 | <职责二> | P0/P1/P2 | <触发频率> | <交付物> |
| 3 | <职责三> | P0/P1/P2 | <触发频率> | <交付物> |

### 6.2 Flow Design

描述新流程。优先用短代码块表达控制流，再补充关键约束。

```text
<input>
  -> <normalize/validate>
  -> <core operation>
  -> <persist/export/return>
```

设计原则：

1. <原则一>
2. <原则二>
3. <原则三>

### 6.3 Configuration / API Design

列出新增或修改的配置、CLI、环境变量、函数签名或公开接口。

```ini
[Section]
field_a=<value>
field_b=<value>
```

优先级：

1. <最高优先级输入>
2. <次级输入>
3. <默认行为>

校验规则：

1. <规则一>
2. <规则二>
3. <规则三>

### 6.4 Data Model / Core Rules

描述核心数据结构、转换规则或不变量。

```python
@dataclass(frozen=True)
class <SpecName>:
  field_a: int = 1
  field_b: str = ""
```

核心规则：

1. <不变量一>
2. <不变量二>
3. <不变量三>

### 6.5 Module-Level Changes

按模块说明要改哪里、为什么改、边界在哪里。

| 模块 | 变更 | 边界 |
| --- | --- | --- |
| `<module_a>` | <变更> | <不负责什么> |
| `<module_b>` | <变更> | <不负责什么> |
| `<module_c>` | <变更> | <不负责什么> |

### 6.6 Compatibility

说明默认行为、旧配置、旧接口、旧产物格式是否保持不变。

1. <兼容点一>
2. <兼容点二>
3. <可能破坏点及处理方式>

### 6.7 Observability / Output Artifacts

列出日志、导出文件、指标、错误信息或调试产物。

| 产物 | 路径/接口 | 说明 |
| --- | --- | --- |
| <产物一> | `<path_or_api>` | <说明> |
| <产物二> | `<path_or_api>` | <说明> |

## 7. Interfaces / Contracts

| 接口 | 说明 | Contract |
| --- | --- | --- |
| `<interface_a>` | <说明> | <必须满足的契约> |
| `<interface_b>` | <说明> | <必须满足的契约> |
| `<interface_c>` | <说明> | <必须满足的契约> |

## 8. Alternatives Considered

### Option A — <备选方案>

Pros：

1. <优点一>
2. <优点二>

Cons：

1. <缺点一>
2. <缺点二>

结论：<采用/不采用原因>。

### Option B — <备选方案>

Pros：

1. <优点一>
2. <优点二>

Cons：

1. <缺点一>
2. <缺点二>

结论：<采用/不采用原因>。

## 9. Risks / Trade-offs

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| <风险一> | <影响> | <缓解措施> |
| <风险二> | <影响> | <缓解措施> |
| <风险三> | <影响> | <缓解措施> |

## 10. Validation / Testing Plan

| 信号 | 成功标准 | 度量方式 |
| --- | --- | --- |
| <测试信号一> | <成功标准> | <测试/命令/Review 方式> |
| <测试信号二> | <成功标准> | <测试/命令/Review 方式> |
| <测试信号三> | <成功标准> | <测试/命令/Review 方式> |

新增测试建议：

```text
tests/unit/<test_name>.py
tests/integration/<test_name>.py
```

手动验证命令：

```bash
<command>
```

## 11. Rollout / Migration Plan

| 阶段 | 动作 | 时间 | 依赖 |
| --- | --- | --- | --- |
| Phase 0 | RFC Review，确认范围和接口 | T+0 | 本 RFC |
| Phase 1 | <第一阶段动作> | T+1 | <依赖> |
| Phase 2 | <第二阶段动作> | T+2 | <依赖> |
| Phase 3 | <第三阶段动作> | T+3 | <依赖> |

迁移策略：

1. <默认不启用/默认保持旧行为>。
2. <用户如何显式启用新行为>。
3. <不兼容配置如何报错或降级>。
4. <回滚方式>。

## 12. Task Breakdown

| Owner | 任务域 | 范围 | 交付物 |
| --- | --- | --- | --- |
| <Owner A> | <任务域> | <范围> | <交付物> |
| <Owner B> | <任务域> | <范围> | <交付物> |
| <Owner C> | <任务域> | <范围> | <交付物> |

任务切分规则：

1. <规则一>
2. <规则二>
3. <规则三>

## 13. Open Questions

1. <待确认问题一>？
2. <待确认问题二>？
3. <待确认问题三>？

## 14. Decision Log

| 日期 | 决策 | 决策人 | 备注 |
| --- | --- | --- | --- |
| YYYY-MM-DD | RFC 创建，Status = Draft | <Owner> | 待 Review |